{
  "name": "@bluestep-systems/b6p-cli",
  "version": "0.6.1",
  "description": "Command-line interface for BlueStep B6P script management",
  "license": "MIT",
  "homepage": "https://github.com/Bluestep-Systems/b6p-cli#readme",
  "bugs": {
    "url": "https://github.com/Bluestep-Systems/b6p-cli/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Bluestep-Systems/b6p-cli.git"
  },
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "b6p": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "compile": "node esbuild.js --production",
    "build:sea": "node scripts/build-sea.mjs",
    "watch": "node esbuild.js --watch",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "test": "node esbuild.test.js",
    "clean": "rm -rf dist dist-test",
    "format": "prettier --write \"src/**/*.ts\"",
    "format-check": "prettier --check \"src/**/*.ts\"",
    "prepublishOnly": "npm run clean && npm run check-types && npm run compile"
  },
  "devDependencies": {
    "@bluestep-systems/b6p-core": "^0.6.0",
    "@types/node": "^22.20.1",
    "commander": "^14.0.3",
    "esbuild": "^0.27.4",
    "prettier": "^3.9.6",
    "typescript": "5.9.2"
  }
}
